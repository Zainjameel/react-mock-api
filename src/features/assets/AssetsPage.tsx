import Typography from "@mui/material/Typography";

export function AssetsPage() {
  return (
    <>
      <Typography variant="h5" sx={{ mb: 2 }}>
        Assets
      </Typography>
      <Typography>Example page (extend with table + details).</Typography>
    </>
  );
}
